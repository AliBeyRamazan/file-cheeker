# file-cheeker

`file-cheeker` is a Go command-line file analysis tool for inspecting binaries and documents. It includes helpers for file type detection, hashes, entropy checks, PE/ELF parsing, string extraction, and rule-based indicators.

## Usage

```powershell
go run . <file_path>
```

## Requirements

- Go 1.22.2 or newer

